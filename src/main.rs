mod days;
mod macros;
mod parse;
mod traits;

use days::*;

fn main() {
    run_day!(01);
    run_day!(02);
    run_day!(03);
    run_day!(04);
    run_day!(05);
    run_day!(06);
    run_day!(07);
    run_day!(08);
    run_day!(09);
    run_day!(10);
    run_day!(11);
    run_day!(12);
    run_day!(13);
    run_day!(14);
    run_day!(15);
    run_day!(16);
    run_day!(17);
    run_day!(18);
    run_day!(19);
    run_day!(20);
    run_day!(21);
    run_day!(22);
    run_day!(23);
    run_day!(24);
    run_day_p1!(25);
}
